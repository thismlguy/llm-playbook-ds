langchain==0.0.177  # there is no stable release yet, so using a hard pin
pandas>=2.0.1,<3
openai>=0.27.7,<0.28
typing-inspect==0.8.0  # required for langchain
typing_extensions==4.5.0  # required for langchain
